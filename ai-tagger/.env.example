# Broker & cache
REDIS_URL=
PORT=
# Optional OCR (needs poppler + tesseract if true)
OCR_ENABLED=

# Version label (returns in API; helpful for audits)
TAGGER_VERSION=

# Optional LLM reranker (set a key to enable)
OPENAI_API_KEY=
LLM_MODEL=

STRUCTURED_LLM_ENABLED=
STRUCTURED_LLM_MODEL=
STRUCTURED_LLM_TIMEOUT_S=
STRUCTURED_LLM_MAX_CHARS=
STRUCTURED_LLM_MAX_UNITS=

OCR_LANGS=
OCR_PDF_DPI=
OCR_MAX_PAGES=
OCR_PDF_MIN_CHARS=
OCR_IMAGE_MAX_SIDE=
OCR_IMAGE_MIN_CHARS=
