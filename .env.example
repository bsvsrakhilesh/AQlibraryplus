# Compose-level variables used before service env_file values are loaded.
# Copy to .env on the production host or export these in the shell.
POSTGRES_PASSWORD=
CORS_ORIGINS=https://your-domain.example
